/*{
  "routes": [
    { "src": "/(.*)", "dest": "/main.jsx" }
  ],
   "build": {
    "env": {
      "GIT_SUBMODULE_STRATEGY": "recursive"
    }
  }
}*/


{
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "dist"
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}
